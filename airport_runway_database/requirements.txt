# Core dependencies (none required for basic functionality)
# The database can be built and queried using only Python standard library

# Optional: For satellite imagery fetching
requests>=2.28.0

# Optional: For historical imagery via Google Earth Engine
# earthengine-api>=0.1.350

# Optional: For data analysis
# pandas>=1.5.0
